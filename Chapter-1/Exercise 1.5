In applicative order of evaluation you evaluate the argument subexpressions first.
Therefore we cannot evaluate (p), causing an infinite loop.
Using normal order you do not touch the argument subexpressions causing it to produce 0.
